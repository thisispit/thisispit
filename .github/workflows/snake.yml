name: snake
on: schedule: [{cron: "0 0 * * *"}]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: Platane/snk@v3
        with:
          github_user_name: thisispit
          outputs: dist/github-contribution-grid-snake.svg
